// Q. Array Traversal

// Ans. Array traversal is the process of visiting each element in an array once.

const fruits = ["Apple", "Banana","Cherry"];

for(let i = 0; i < fruits.length; i++){
    console.log(fruits[i]);
}
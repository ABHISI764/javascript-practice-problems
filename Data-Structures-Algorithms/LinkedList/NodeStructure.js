// Node Structure
// Each node contains a value and a next pointer. The list starts at head and ends where next is null.

class Node {
    constructor(value) {
        this.value = value;
        this.next = null;
    }
}

